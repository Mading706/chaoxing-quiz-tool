{
  "name": "chaoxing-quiz-tool",
  "version": "1.46.0",
  "private": true,
  "description": "Tampermonkey userscript for parsing Chaoxing quizzes and exporting Kaoshibao-compatible Excel, Word and PDF files.",
  "scripts": {
    "check": "npm run check:syntax && npm run check:metadata",
    "check:syntax": "node --check chaoxing-quiz-tool.user.js",
    "check:metadata": "node scripts/check-userscript-metadata.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
